{
  "name": "ai-services",
  "version": "1.0.0",
  "private": true,
  "description": "AI systems landing page for music companies.",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "check": "python3 _assemble.py --check && node scripts/check-site.mjs",
    "build": "python3 _assemble.py"
  },
  "engines": {
    "node": ">=20"
  }
}
